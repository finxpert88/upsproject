# 修订9交付整理与验证记录

日期：2026-09-15，Asia/Singapore。授权来源：用户确认本轮FSD、其他文档及DEMO全部评审通过，Leader将目录与Git写操作统一委派本任务。此为用户评审确认，未虚构新增独立评审报告或生产测试。

## 保留与整理

- 保留PRD/FSD修订9、RTM、原始原型及ZIP、历史r7/r8评审和离线验证附件、DEMO全部源码、测试和字体许可证。`UPS-FSD-R4`实际为当前修订9实现，保留路径及全部构建入口。
- 添加根README，明确当前入口、原始参考、证据和产品边界；更新PRD/FSD状态及RTM的当前确认，旧矩阵保留历史身份。
- 将原`tmp/checks*.json`移动到`historical-checks/`，将原`tmp/verified/`移动到`report-evidence/`。其中checks-r9及files-r9对应本轮重新运行；旧版本JSON与旧渲染图仅为历史证据。报告文件本轮重新生成和解析，旧截图不得当作本轮视觉检查。
- 添加根忽略规则，排除依赖、Python缓存及环境凭据文件；原DEMO的tmp忽略规则继续有效。
- 未删除任何跟踪文件或Git历史。临时删除操作被自动审批以`blocked by policy`拒绝，未绕过。`pending-cleanup-files.json`列出原tmp根层待清理文件；tmp/localization及其依赖、tmp/node_modules外部运行时junction、空operations目录也仍保留在本地。它们不进入本次Git提交。外部运行时未修改。

## 本轮执行证据

基线实现：`07456ff`，Windows / Node v24.19.0 / Python 3.12.14。执行目录为`doc/UI prototype/UPS-FSD-R4`。

|检查|结果|边界|
|---|---|---|
|node build.cjs|通过；重新生成单文件HTML，与已提交产物一致|构建检查|
|node verify.cjs|34/34通过|Node VM与DOM桩|
|node verify.cjs --single-file|34/34通过|实际内嵌脚本的Node VM执行|
|python verify-files.py|9/9允许的PDF/XLSX/CSV解析通过|字体、英文、类型、时区、模拟标记、边界和记录数；非页面实屏验收|

Node检查详情见[checks-r9.json](historical-checks/checks-r9.json)，文件检查详情见[files-r9.json](report-evidence/files-r9.json)。PDF渲染属于报告输出检查，不是DEMO页面截图。移动的是测试输出，测试源仍按原路径重新生成tmp，因此不改构建或测试代码。

## 验收边界与遗留

用户文档/DEMO评审通过已登记。浏览器真实视口、触控、软键盘、200%缩放、iOS/iPadOS保存及FIN生产后台、安全权限、现场集成和真实Telegram验收未在本次执行；沿用[响应式验证矩阵](../UI%20prototype/UPS-FSD-R4/RESPONSIVE-VALIDATION.md)及[DEMO说明](../UI%20prototype/UPS-FSD-R4/README.md)的限制。

本轮只在当前`feature/fsd-r4-static-demo`分支正常提交推送，不合并main、不force push、不覆盖标签。带注释标签在创建时从系统当前时间转换Asia/Singapore，以yyyyMMddHHmmss命名；最终提交、标签和远端核验由交付回复给出。
