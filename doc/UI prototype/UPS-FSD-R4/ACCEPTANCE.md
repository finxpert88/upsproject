# 静态DEMO验证记录

当前依据：共享工作区FSD v0.1修订9，第21节LANG-01～04及第22节RESP-01～06。验证日期2026-09-15（Asia/Singapore）。修订6、7、8的交付/验证分别独立保存在VALIDATION-R6.md、VALIDATION-R7.md、VALIDATION-R8.md，修订8提交5f959a7。旧结果不当作本轮响应式通过证据。

## 本轮已执行

`node build.cjs`完成独立单文件构建；`node verify.cjs --single-file`直接执行实际交付HTML内嵌脚本，通过同一34项检查。此项确认资源依赖和内嵌脚本顺序，不等同于浏览器实屏验收。

`node verify.cjs`：34项本地检查通过。保留24项功能检查及3项英文验证，增加7项响应式逻辑检查：22组合成尺寸变化保持状态/监听器/定时器；Menu与焦点函数；工程文本及复选框草稿恢复；告警全部列标签；无需hover的趋势取样；文件分享取消/过期/重置链接；允许缩放及移除旧隐藏列/CSS中文标签。详细证据与限制见RESPONSIVE-VALIDATION.md。这些是Node VM/DOM桩检查，不是浏览器视口、触控或真机测试。

`python verify-files.py`：使用pypdf/pdfplumber/openpyxl及CSV解析器重新打开全部9种允许输出。核对英文标题/表头/内容、记录数、统计值、数值/日期类型、冻结表头、代次/模拟标记、Asia/Singapore时区、内嵌字体及字符边界。输入为2台设备，概览PDF2页、告警PDF1页、当前维护PDF1页；4个XLSX分别7/7/5/5张表；指标CSV2308行、告警CSV9行。文件来自相同ReportEngine，不是截图或空链接。

修订8曾逐页检查PDF4页及工作簿24张表关键区域，并修复英文换行与Excel行高；记录保留于VALIDATION-R8.md。修订9沿用独立导出器并重跑9文件解析，不把旧版文件截图或主题对比度当作新响应式页面实屏证据。

## RESP-01～06交付状态

布局、Menu、44px声明、完整移动表卡、触控取样、草稿保持、弹窗安全区及移动文件入口已实现。完整代表尺寸、横竖屏、iPad分屏、200%缩放、截图与浏览器/OS版本矩阵见RESPONSIVE-VALIDATION.md。

**没有生成本轮响应式页面截图，没有真实浏览器视口模拟或手机/iPad操作记录。** 原本地文件安全策略仍阻止浏览器读取，未采用localhost/代理等方式绕过。RESP-01～06均不标整项发布验收通过；无整页溢出、44px实际几何、软键盘和iOS保存等必须补充浏览器/真机证据。

## 英文验收对应范围

|条目|本轮证据|尚未完成的范围|
|---|---|---|
|LANG-01|lang=en-US；11个路由、32个指标弹窗、浅深主题渲染字符串无中文/未解析资源；集中英文资源；长标签允许换行|真实浏览器DOM/tooltip/所有确认弹窗、移动端、200%缩放、键盘实屏验收。VM字符串不等于实屏，无整项通过结论|
|LANG-02|7类场景直接生成英文，运行/暂停/恢复/重置/再生成；历史、资产、计划、告警快照与英文错误；旧中文存储隔离；OP-01 en-US|所有异常组合与后台恢复/迁移未穷尽；本地检查通过不代表FIN服务端验证|
|LANG-03|4模板9种输出重新解析；PDF逐页检查，24张工作表关键区域检查；英文、数值类型、时区、模拟标识与字体边界|长工作簿全部行列人工逐屏与目标Office版本兼容仍需补验，不标为完整发布门槛通过|
|LANG-04|live/simulation模板的发生/恢复/确认/严重度变化/测试正文；经核对英文映射、未核对回退、稳定ID、级别/生命周期保留；本地通知正文带Simulated Data及空messageId|真实服务端保存原始证据、英语映射审批、OP-01平台响应和Telegram投递未接入|

运行证据输出到被Git忽略的`tmp/checks-r9.json`、`tmp/verified/files-r9.json`及相应报告/PNG。没有将旧结果改名当作英文或响应式通过。当前DEMO尚不满足全部发布门槛，尤其浏览器实屏、真机及真实后端部分。

## 对修订7验收的对应范围

|条目|本地证据|尚未完成的范围|
|---|---|---|
|R7-01|管理入口拒绝1.2、未知字段；2.0上下文及会话代次|完整FIN Snapshot/Entity、OP-15～22/31线协议及后端校验尚未实现，不标整项通过|
|R7-02|read/ack/export/manage及本地会话ACL区分，live权限不替代|真实登录、服务端ACL持久化与跨用户授权验收|
|R7-03|模拟发生/确认/恢复独立，已恢复发生不被再次清除|FIN outbox的CREATE/CLEAR未知结果与重启串行投递|
|R7-04|运行报告遇reset即取消；旧provider拒绝；重启续清理；删除墓碑|服务器事务、磁盘工作文件、在途下载中断与进程崩溃测试|
|R7-05|前端文档按FR-25/修订7标识；旧版本记录隔离|PRD/FSD追踪矩阵由规划侧维护|
|R7-06|tick仅增加dataRevision；旧controlRevision暂停成功；reset后未生成禁止step|后台进程重启/故障策略的平台测试|
|R7-07|60秒单步、60次1秒及1/5/10倍值/事件相同，包含中途质量触发和日历跨日|初始批量历史与完整后台调度器统一、持久水位崩溃恢复|
|R7-08|carry-in/freshness/映射边界裁剪；截止之后确认不进入过去报告；未来范围处理|真实映射多版本、缺段生命周期与平台历史权威性|
|R7-09|真实模式表单呈现独立紧急暂停原因、resumePauseRevision，未接入按钮禁用|OP-31持久发送许可闸门、发布CAS竞态与真实在途处理|
|R7-10|unsupported/unmapped来源与时间可空；stale保留sim来源且当前值为空|目标FIN源绑定及完整Metric服务器schema验收|

## 明确未验

- 页面浏览器实屏、双主题1440/1366/390宽、200%缩放、键盘及图表悬停：先前本地文件安全策略拒绝，本轮未绕过。VM字符串检查不等于浏览器视觉验收。
- M-15 FIN后台、完整OP契约、平台权限/审计/发布/报告/Telegram；没有连接现场或外发消息。
- 初始历史批量生成与全事件调度统一、长时间性能和后台事务恢复。

结论：可交付有数据的静态DEMO及已验证的本地模拟流程；不能宣称完整FSD 1:1产品功能或R7-01～10整套验收通过。
