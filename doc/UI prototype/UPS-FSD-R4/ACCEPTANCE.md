# 验收记录

依据：当前FSD修订5。

## 已检查

2026-09-14运行 `node verify.cjs`，10项代码级检查通过：

- 首次浅色；按项目/用户保存和隔离主题，存储失败仍可当次切换。
- 平台适配未配置时设备、告警、报表任务集合为空，快照为空。
- Dashboard六卡顺序及32项说明入口保留。
- 未取得数据时健康环、量表、历史框与告警计数保持中性或未知。
- 所有功能页面可在无平台连接时渲染。
- 四报表模板与支持格式对应，执行/下载不提供虚假结果。
- 通知渠道未创建、不提供成功投递记录或token输入。
- 运行源码没有固定业务数据、随机数填充或本地执行成功路径。
- 12组主要文字/面板token对比度计算达到4.5:1。
- 单文件构建完成；资源全部本地内嵌。

## 待验

- 两主题在1366/1440/移动宽度、200%缩放下的实际视觉、键盘和图表交互。
- 所有真实FIN读取/写入适配、权限范围、质量时效与生命周期验证。
- 报表生成、PDF/XLSX/CSV实际导出、任务取消/过期/下载撤权。
- Telegram受控测试、渠道发布、通知队列、失败/未知结果与审计。

浏览器先前拒绝本地文件访问。本轮没有绕过，不把标记或对比度计算称为实屏验收通过。
